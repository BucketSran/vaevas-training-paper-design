# Build clean-room training data

The training paper will rebuild its training set as clean-room data with explicit provenance, contamination audit, and verifier evidence for every example. Previous experiment outputs are not training data by default; they may guide error taxonomies or pipeline design, but they enter the dataset only after item-level re-audit and validation.
