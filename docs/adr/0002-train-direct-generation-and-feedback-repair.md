# Train direct generation and feedback repair

The training paper will evaluate two modes: direct generation from a specification and feedback repair from a specification plus broken artifacts plus verifier feedback. This makes verifier feedback a learned capability rather than only a hidden scoring mechanism, and lets the paper separately report first-attempt correctness and repair effectiveness.
